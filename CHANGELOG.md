# Changelog

## [Unreleased]

### Added

- 开放 `[persona]` 下的 `how_you_speak`、`how_you_act`、`when_to_stop` 三个系统提示词区块自定义编辑。
- 用户未填写或填写空白时继续使用插件内置原始模板；只覆盖单个区块时，其余区块保持默认内容。

### Changed

- 自定义内容作为对应区块正文注入，并由插件保留结构化标签，避免用户配置破坏提示词边界。


## [0.5.3] - 2026-08-24

### Fixed

- 回合结束时重新对账 claim 与当前 unread/history 状态，支持消息已被其他路径提前移入 history 的情况，避免重复写入历史或误判消息丢失。
- 消息确认按“同一对象实例 → 唯一平台复合身份 → 唯一完整指纹”逐级匹配；发现多个候选时拒绝确认，不再猜测消息归属。
- 修复合并窗口释放 mailbox owner 后 finally 阶段重复释放的问题。

### Tests

- 增加重复复合身份、同一对象优先、unread/history 混合对账及提交前消息移动场景的回归测试。


## [0.5.2] - 2026-08-24

### Fixed

- 修复 mailbox claim 确认与 stream 未读/历史上下文交错更新时可能发生的部分确认；先完整匹配，再在同一 claim 提交中应用变更。
- claim 匹配缺失、提交失败或回合取消时保留可重试状态，避免已领取消息被错误确认或丢失。
- 相同消息对象重复出现在未读队列时，仅确认与本轮 claim 数量对应的项目，不再批量移除重复引用。

### Changed

- 增加 mailbox owner、claim 释放和 claim 提交失败的结构化诊断日志，便于定位回合确认异常。

## [0.5.1] - 2026-08-22

### Added

- 新增默认关闭的 `[tools].record_provider_error_request_body` 诊断开关；命中供应商异常正文拦截时，可将插件可见的受控请求上下文追加到插件专属 JSONL。
- 记录会将 system 中已知的人设、身份、背景和回复风格替换为稳定变量，并保留受限长度的用户输入、工具定义、Tool Call 和 Tool Result，便于复盘。

### Security

- 供应商异常请求体记录不包含 reasoning 或媒体二进制/base64，并脱敏工具参数和结果中的常见凭据字段。

## [0.5.0] - 2026-08-21

### Added

- 新增主 Agent 子决策 JSON 硬拦截，避免 `sub_actor` 的 `action`、`confidence`、`reason_codes` 等内部判断结构被误作为聊天正文发送。
- 新增安全拦截日志 `event=reply_decision_json_intercepted`，仅记录动作和长度，不记录内部 JSON 原文。

### Changed

- 收紧决策 JSON 检测：校验完整字段类型、数值范围、有限数值和原因码类型，降低正常结构化内容被误拦截的风险。
- 保留工具、act 和决策日志的英文结构化字段，并补充中文说明，便于检索和人工排查。

## [0.4.0] - 2026-08-19

### Added

- 新增 `[tools].tool_call_mode` 工具调用模式配置，默认使用 `planning`。
- `planning` 模式按模型规划顺序逐个向 Core 提交 Tool Call，适合存在真实结果依赖的多步任务。
- `batch` 模式将本轮普通 Tool Call 整批交给 MoFox Core 调度，减少插件侧的分批等待和 LLM 往返。
- 在 Agent 系统提示词中补充两种模式的调用规则、工具依赖顺序和结果回灌边界。

### Changed

- 工具鼓励提示明确区分独立工具组合与依赖工具分轮调用。
- README 增加工具调用模式配置说明。

### Notes

- `batch` 仅表示整批交给 Core 调度，不保证绝对并行、严格 FIFO 或固定执行顺序。
- 依赖前置工具真实结果、生成 ID 或执行状态的调用，仍应等待 `Tool Result` 后在下一轮规划。

## [0.3.2] - 2026-08-19

### Added

- 新增连续输入合并窗口，短时间内到达的消息可在下一轮统一处理。
- 新增连续中断次数限制，避免高频消息持续取消模型生成。
- 新增可配置工具调用日志开关，开启后记录工具名和脱敏后的参数。
- 为 Agent act、工具调用、工具结果和 mailbox 生命周期日志补充中文说明，同时保留英文结构化字段，便于检索。

### Changed

- 完善输入合并、中断释放、回合确认和连续中断计数的诊断日志。

## [0.3.1] - 2026-08-19

### Added

- 新增供应商异常正文检测：识别 Google/Gemini 审核拦截被错误包装为正常模型文本的情况。
- 命中明显审核拦截文本时，在完整分段发送前阻止消息外发，并记录不包含原文的结构化日志。

### Fixed

- 修复上游返回 `The prompt could not be submitted...` 等审核说明时，被 Agentic Chatter 当作正常回复发送的问题。

## [0.3.0] - 2026-08-14

### Added

- 新增供应商异常正文检测：识别 Google/Gemini 审核拦截被错误包装为正常模型文本的情况。
- 命中明显审核拦截文本时，在完整分段发送前阻止消息外发，并记录不包含原文的结构化日志。

### Fixed

- 修复上游返回 `The prompt could not be submitted...` 等审核说明时，被 Agentic Chatter 当作正常回复发送的问题。

## [0.3.0] - 2026-08-14

### Added

- 新增按聊天流隔离的连续输入 mailbox：同一 stream 同时仅允许一个回合运行；生成期间的新消息进入下一轮输入。
- 新增输入 claim、release 与 commit 生命周期：本轮输入固定，失败或新消息中断时恢复原 claim；成功时仅确认当前 claim，不会误 flush 后到消息。
- 新增工具调用账本，记录本轮工具的成功、失败或跳过状态，以及可安全回显的结果摘要。

### Changed

- 工具结果回灌改为按调用 ID 优先匹配，并在无法可靠匹配时显式标记为缺失或歧义，不猜测结果归属。
- 统一 act 循环的继续与终止裁决；停止会话、结束回合、纯文本回复、重复文本和各类循环上限均有明确优先级与结束原因。

### Fixed

- 修复生成期间新消息与当前回合输入竞争时可能发生的重复处理、漏读或误确认问题。
- 修复工具失败仍可能被视为有效进展、以及完整工具结果未被可靠回灌后续循环的问题。

## [0.2.3] - 2026-08-12

### Changed

- 强化主 Agent 的工具使用指引：需要查询、读取、计算、记忆、表情或外部动作时优先调用实际可用的工具。
- 普通文本发送且没有普通工具调用时自动结束本轮；只有工具结果带来新信息时才允许补充正文。
- 移除以用户消息形式注入的发送后提示，避免产生“好的”“不会重复”等内部流程确认型尾句。

### Fixed

- 修复 QQBot 流式输出检测到相似回复后仍被计为有效进展、进而继续生成重复内容的问题。
- 修复工具执行失败仍被计为有效进展，导致无进展保护无法及时终止的问题。
- 修复旧版核心未公开导出 `ChatterResult` 时插件整体加载失败的问题。
- 增加 act 迭代、工具执行结果与自动结束原因的安全诊断日志。

## [0.2.2] - 2026-08-11

### Added

- 新增可独立启停的私聊 Chatter 接管开关，默认保持接管私聊。
- 支持通过 `config/model.toml` 中的 `[[models]].name` 为私聊主 Agent 指定单模型。
- 将群聊、私聊和讨论组拆分为精确 `ChatType` 组件，关闭私聊接管时可交由其他兼容 Chatter 处理。

### Changed

- 私聊指定模型继承主模型任务的 `temperature` 与 `max_tokens`，且仅覆盖 `act` 主回复循环，不影响其他辅助模型任务。

## [0.2.1] - 2026-08-11

### Fixed

- 拦截模型完整复述的框架格式消息行，避免 Bot 自身消息回显被再次发送。
- 拦截时输出不含消息正文和用户标识的安全诊断日志。

## [0.2.0] - 2026-08-11

### Added

- 支持通过框架 `StreamEvent.text_delta` 获取真实 LLM 增量，并在 QQBot C2C 私聊实时更新同一条消息。
- 支持流式 Service 配置、首发字符阈值与最小更新字符阈值。

### Fixed

- 流式输出过滤结构化 reasoning 及正文中的明确思考标签，避免将思考内容发送给用户。
- 在流式启动失败时回退普通发送，并在已启动后的中断或异常路径收尾，避免重复消息。

## [0.1.0] - 2026-08-01

### Added

- 提供 `perceive`、`decide`、`plan`、`act`、`reflect` 可编排 Agent 对话管线。
- 提供硬规则、本地评分、`sub_actor` 灰区判断和故障回退组成的分层回复决策。
- 支持人格昵称与别名召唤、对话节奏控制和可解释决策日志。
- 提供工具渐进披露、工具调用去重和 Agent 循环终止保护。
- 提供语义分段、打字延迟、情绪状态、打断重规划和跨流全局心智。
- 提供 `PipelineService`、`ExploreToolsTool` 及消息发送和回合控制 Action。
